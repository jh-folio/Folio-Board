{
  "name": "folio-board-web",
  "private": true,
  "version": "0.7.0",
  "type": "module",
  "description": "Folio Board React island shell (gradual adoption). Builds fixed-name bundles into ../public/react for the FastAPI static server to serve.",
  "scripts": {
    "dev": "vite build --watch",
    "build": "vite build",
    "test": "npm run test:source && npm run test:unit",
    "test:source": "node --test tests/*.test.mjs",
    "test:unit": "vitest run",
    "test:ui": "playwright test",
    "typecheck": "tsc --noEmit",
    "build:vendor": "node scripts/build-vendor.mjs"
  },
  "dependencies": {
    "pixi.js": "^8.19.0",
    "react": "^19.2.8",
    "react-dom": "^19.2.8"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.12.1",
    "@playwright/test": "^1.62.1",
    "@types/react": "^19.2.18",
    "@types/react-dom": "^19.2.4",
    "@vitejs/plugin-react": "^6.0.5",
    "echarts": "6.1.0",
    "lightweight-charts": "5.2.0",
    "react-doctor": "^0.9.3",
    "react-grab": "0.1.50",
    "react-scan": "0.5.7",
    "typescript": "^7.0.2",
    "vite": "^8.1.5",
    "vitest": "^4.1.11"
  }
}
